<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home | Blogify</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
  <style>
    /* ==========  CORE TOKENS  ========== */
    :root {
      --primary-color: #3498db;
      --secondary-color: #2c3e50;
      --success-color: #27ae60;
      --danger-color: #e74c3c;
      --dark-bg: #1a1a1a;
      --card-bg: #242424;
      --text-primary: #ffffff;
      --text-secondary: #b0b0b0;
      --border-color: #333;
      --hover-color: #3a3a3a;
      --ease-out: cubic-bezier(0.34, 1.56, 0.64, 1);
      --ease-in-out: cubic-bezier(0.45, 0, 0.55, 1);
    }

    /* ==========  RESET  ========== */
    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      background: var(--dark-bg);
      color: var(--text-primary);
      font-family: 'Segoe UI', Arial, sans-serif;
      line-height: 1.6;
    }

    .main-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 5rem 1rem 2rem;
    }

    h1 {
      font-size: 2rem;
      margin-bottom: 1.5rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    /* ==========  ALERTS  ========== */
    .alert {
      padding: 1rem;
      margin-bottom: 1.5rem;
      border-radius: 8px;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    .alert-success { background: rgba(39, 174, 96, .15); color: var(--success-color); }
    .alert-error   { background: rgba(231, 76, 60, .15);  color: var(--danger-color); }

    /* ==========  BLOG-CARD  ========== */
    .blog-card {
      background: var(--card-bg);
      border: 1px solid var(--border-color);
      border-radius: 12px;
      padding: 1.5rem;
      margin-bottom: 2rem;
      transition: transform .35s var(--ease-out),
                  box-shadow .35s var(--ease-out);
      overflow: hidden;
    }
    .blog-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 20px rgba(0,0,0,.45);
    }

    .blog-card img {
      width: 100%;
      border-radius: 8px;
      margin-bottom: 1rem;
      transition: transform .5s var(--ease-out);
      transform-origin: center;
    }
    .blog-card:hover img { transform: scale(1.04); }

    .blog-card h2 a {
      font-size: 1.5rem;
      color: var(--primary-color);
      text-decoration: none;
      position: relative;
      background-image: linear-gradient(var(--primary-color), var(--primary-color));
      background-repeat: no-repeat;
      background-position: left 100%;
      background-size: 0 2px;
      transition: background-size .35s var(--ease-out);
    }
    .blog-card:hover h2 a { background-size: 100% 2px; }

    .blog-excerpt {
      color: var(--text-secondary);
      margin-bottom: 1rem;
    }

    .blog-meta {
      font-size: 0.9rem;
      color: var(--text-secondary);
      margin-bottom: 1rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    .blog-meta a { color: var(--primary-color); text-decoration: none; }
    .blog-meta a:hover { text-decoration: underline; }

    /* ==========  BUTTONS  ========== */
    .blog-actions {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
      padding-top: 1rem;
      border-top: 1px solid var(--border-color);
    }

    .action-btn,
    .follow-btn,
    .view-btn {
      padding: 0.5rem 1rem;
      border: none;
      border-radius: 6px;
      color: #fff;
      font-size: 0.9rem;
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      transition: transform .2s var(--ease-out),
                  box-shadow .2s var(--ease-out),
                  background .2s var(--ease-out);
    }

    .action-btn        { background: var(--primary-color); }
    .action-btn.liked  { background: var(--danger-color); }
    .action-btn:hover  { background: #2980b9; transform: scale(1.05); }

    .follow-btn            { background: var(--success-color); }
    .follow-btn.following  { background: var(--danger-color); }
    .follow-btn:hover      { transform: scale(1.05); }

    .view-btn            { background: var(--primary-color); text-decoration: none; }
    .view-btn:hover      { background: #2980b9; transform: scale(1.05); }

    /* ==========  COMMENTS  ========== */
    .comments-section {
      margin-top: 1.5rem;
      padding-top: 1.5rem;
      border-top: 1px solid var(--border-color);
    }
    .comment {
      display: flex;
      gap: 1rem;
      margin-bottom: 1rem;
      padding: 1rem;
      background: var(--hover-color);
      border-radius: 8px;
    }
    .comment img {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      object-fit: cover;
    }
    .comment-content { flex: 1; }
    .comment-author  { font-weight: bold; }
    .comment-text    { color: var(--text-secondary); margin: 0.25rem 0; }
    .comment-time    { font-size: 0.8rem; color: var(--text-secondary); }

    /* ==========  RESPONSIVE  ========== */
    @media (max-width: 768px) {
      .main-container { padding: 4rem 1rem 1rem; }
      h1 { font-size: 1.5rem; }
      .blog-card h2 a { font-size: 1.2rem; }
      .blog-actions { flex-direction: column; gap: 0.5rem; }
    }

    /* ==========  ENTRANCE ANIMATION  ========== */
    .blog-card {
      animation: slideUp .6s var(--ease-out) backwards;
    }
    .blog-card:nth-child(1) { animation-delay: .05s; }
    .blog-card:nth-child(2) { animation-delay: .1s; }
    .blog-card:nth-child(3) { animation-delay: .15s; }
    .blog-card:nth-child(4) { animation-delay: .2s; }

    @keyframes slideUp {
      from { opacity: 0; transform: translateY(20px); }
      to   { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>
  <%- include('partials/header', { user }) %>

  <div class="main-container">
    <h1><i class="fas fa-blog"></i> Latest Blogs</h1>

    <% if (success_msg) { %>
      <p class="alert alert-success"><i class="fas fa-check-circle"></i> <%= success_msg %></p>
    <% } %>
    <% if (error_msg) { %>
      <p class="alert alert-error"><i class="fas fa-exclamation-circle"></i> <%= error_msg %></p>
    <% } %>

    <% blogs.forEach(blog => { %>
      <div class="blog-card">
        <% if (blog.coverImage) { %>
          <img src="<%= blog.coverImage %>" alt="Cover Image">
        <% } %>
        <h2><a href="/blog/<%= blog._id %>"><%= blog.title %></a></h2>
        <p class="blog-excerpt"><%= blog.body.substring(0, 200) %>...</p>
        <div class="blog-meta">
          Posted by <a href="/profile/<%= blog.createdBy._id %>"><i class="fas fa-user"></i> <%= blog.createdBy.fullname %></a>
          <i class="fas fa-clock"></i> <%= moment(blog.createdAt).fromNow() %>
        </div>

        <div class="blog-actions">
          <% if (user) { %>
            <form action="/blog/<%= blog._id %>/like" method="POST">
              <button class="action-btn <%= blog.likes.includes(user._id) ? 'liked' : '' %>" type="submit">
                <i class="fas <%= blog.likes.includes(user._id) ? 'fa-heart' : 'fa-heart-broken' %>"></i>
                <%= blog.likes.includes(user._id) ? 'Unlike' : 'Like' %> (<%= blog.likes.length %>)
              </button>
            </form>

            <form action="<%= blog.isFollowing ? '/user/unfollow/' : '/user/follow/' %><%= blog.createdBy._id %>" method="POST">
              <button class="follow-btn <%= blog.isFollowing ? 'following' : '' %>" type="submit">
                <i class="fas <%= blog.isFollowing ? 'fa-user-minus' : 'fa-user-plus' %>"></i>
                <%= blog.isFollowing ? 'Unfollow' : 'Follow' %>
              </button>
            </form>
          <% } %>

          <a href="/blog/<%= blog._id %>" class="view-btn">
            <i class="fas fa-eye"></i> View Post
          </a>
        </div>

        <div class="comments-section">
          <h3><i class="fas fa-comments"></i> Comments</h3>
          <% blog.comments.forEach(comment => { %>
            <div class="comment">
              <img src="<%= comment.createdBy.profileImageURL %>" alt="Profile Image">
              <div class="comment-content">
                <div class="comment-author"><%= comment.createdBy.fullname %></div>
                <div class="comment-text"><%= comment.content %></div>
                <div class="comment-time"><i class="fas fa-clock"></i> <%= moment(comment.createdAt).fromNow() %></div>
              </div>
            </div>
          <% }) %>
        </div>
      </div>
    <% }) %>
  </div>
</body>
</html>
